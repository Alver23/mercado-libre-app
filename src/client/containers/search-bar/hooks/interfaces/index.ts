// Dependencies
import { FormEvent } from 'react';

export type onChangeTextType = (event: FormEvent<HTMLInputElement>) => void;

export type handleClickType = (event: FormEvent<HTMLButtonElement>) => void;
